apiVersion: 1

apps:
  - type: 'velodb-doris-app'
    org_id: 1
    org_name: 'velodb'
    disabled: false
    pinned: true
    jsonData:
      apiUrl: http://default-url.com
    secureJsonData:
      apiKey: secret-key
