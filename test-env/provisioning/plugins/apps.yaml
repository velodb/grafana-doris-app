apiVersion: 1

apps:
  - type: velodb-doris-app
    org_id: 1
    disabled: false
    pinned: true
    jsonData:
      apiUrl: http://localhost
      logsConfig:
        datasource: doris-test
        database: otel
        logsTable: otel_logs
        targetTraceTable: otel_logs
        applicationAttributeKey: app
    secureJsonData:
      apiKey: test-only

